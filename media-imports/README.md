# Media imports

Drop original high-resolution photographs into `high-resolution/` from the VS Code Explorer.

Files in that folder stay on this computer and are intentionally excluded from Git, Vercel, and the public website. The future R2 uploader will create web-sized AVIF/WebP variants and upload those variants to Cloudflare without modifying the originals.

