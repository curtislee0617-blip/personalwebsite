import { SectionLoading } from "@/components/section-loading";

export default function Loading() {
  return (
    <SectionLoading
      variant="cv"
      title="Opening the CV"
      description="The document view is sharpening up."
    />
  );
}
