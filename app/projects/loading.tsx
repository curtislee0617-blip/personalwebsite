import { SectionLoading } from "@/components/section-loading";

export default function Loading() {
  return (
    <SectionLoading
      variant="projects"
      title="Writing..."
      description=""
    />
  );
}
