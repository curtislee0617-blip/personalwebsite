import { SectionLoading } from "@/components/section-loading";

export default function Loading() {
  return (
    <SectionLoading
      variant="privacy"
      title="Opening the note"
      description="A short page is loading."
    />
  );
}
